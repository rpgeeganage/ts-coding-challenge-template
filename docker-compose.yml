version: '3.5'
services:
  my-task:
    build:
      context: .
    container_name: my-task
    env_file:
      - ._env
    command: tail -f /dev/null
    volumes:
      - .:/app
    logging:
      driver: json-file
